# cpp-linear-regression
A clean and minimal C++ implementation of Linear Regression from scratch without external ML libraries. Includes CSV data loading, gradient descent training, prediction functionality, and a fully configured CMake build system.
